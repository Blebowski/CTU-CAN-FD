default:
    log_level: info
    error_tolerance: 0
    iterations: 50
tests:
    bit_stuffing:
        iterations: 10
        wave: Bit_Stuffing/Bit_Stuffing.tcl

    apb:
        iterations: 1
    bus_sync:
    crc:
    fault_confinement:
    int_man:
    mess_filt:
    presc:
    protocol_control:
    rx_buf:
        iterations: 10
    tx_arb:
    tx_buf:
